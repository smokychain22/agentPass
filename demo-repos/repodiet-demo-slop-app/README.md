# repodiet-demo-slop-app

Intentional AI-code-bloat patterns for RepoDiet demo scanning.

This seeded demo workspace contains duplicate components, unused files, orphan modules,
unused dependencies, and safe-delete candidates in `archive/`, `backup/`, `tmp/`, and `old/`.

RepoDiet scans this repo locally when you click **Try Demo Repo** — no fake findings.
