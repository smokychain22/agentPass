// Intentional safe-delete candidate — archive folder only.
export function OldScreen() {
  return (
    <div className="rounded border border-dashed border-gray-400 p-4 text-sm text-gray-500">
      OldScreen — archived experiment, safe to remove.
    </div>
  );
}
